server:
  port: 8080

spring:
  application:
    name: desafioAWS

  cloud:
    aws:
      credentials:
        access-key: Ax
        secret-key: x
      region:
        static: x
      endpoint: x

      sqs:
        queue-name: x
      s3:
        bucket-name: x

